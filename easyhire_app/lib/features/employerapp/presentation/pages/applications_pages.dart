import 'package:flutter/material.dart';

class ApplicationsPages extends StatelessWidget {
  const ApplicationsPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Employer Appn Page"));
  }
}
